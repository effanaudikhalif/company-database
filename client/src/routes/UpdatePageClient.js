import React from "react";
import UpdateClient from "../components/UpdateClient";

const UpdatePageClient = () => {
  return (
    <div className="container-update">
      <h1 className="text-center">Update Client</h1>
      <UpdateClient />
    </div>
  );
};

export default UpdatePageClient;
